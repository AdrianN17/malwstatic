{
  "name": "malwstatic",
  "version": "1.2.5",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "drawflow": "^0.0.60",
    "yaml": "^2.9.0"
  },
  "scripts": {
    "dev": "bun build ./src/init.ts --outfile ./dist/init.js --watch",
    "build": "bun run build.ts",
    "clean": "rm -rf dist build .tsbuildinfo"
  }
}
